# 散步 App — 项目交接文档

给 Claude Code 的上下文。项目已上线运行，这是继续迭代的起点。

---

## 一句话

一个把散步变成拍照任务的小游戏。开局给三个任务，用手机实时取景拍照，AI 用「有点损的朋友」的语气点评，走完生成一张卡片。

---

## 当前状态

**已上线**：https://klu991230.github.io/WalkingApp/
**仓库**：github.com/klu991230/WalkingApp（Public）
**部署**：GitHub Pages，main 分支根目录，push 后一两分钟自动更新
**代码**：`index.html`（约 1300 行，HTML + CSS + 原生 JS）+ `tasks.json`（题库）
+ `proxy/`（中转站，跑在 Cloudflare Workers 上）。网页本体无框架、无构建步骤、无依赖。

v1 已经真机走通。v2 的 1–4 已上线（见下面「v2 进度」），5、6 还没做。

---

## 关于作者（重要）

- **非技术背景**，咨询顾问，不会从零写代码
- 目标是**看懂代码在干什么**，不是自己写
- **给命令和代码之前，先用中文讲清楚每一段在做什么**，讲完再给。他说"just the commands"时才跳过讲解
- 界面配色**偏好浅色、米色系，不要深色**
- 这个项目定位是**自己动手练习**，不是创业项目。别往商业化方向带

---

## 技术约束（都是踩过的坑，别踩回去）

**1. 实时取景必须 HTTPS**
`getUserMedia` 只在 https 或 localhost 下可用。本地测试用 `python3 -m http.server` 起服务后走 `localhost` 可以；用局域网 IP（192.168.x.x）访问会被浏览器拒绝。代码里已有兜底：摄像头调不起来时自动退回系统相机（file input + capture）。**别删这个兜底。**

**2. 不能引外部字体和 CDN**
作者在中国大陆，Google Fonts 等境外 CDN 不可靠。**只用系统字体**，现在是 PingFang SC + ui-monospace。别引入任何 npm 包或 CDN 链接。

**3. API key 绝对不能进仓库**
仓库是 Public。key 由用户填在页面输入框里，存 localStorage。代码里有个 `HARDCODED_KEY` 常量必须保持空字符串。**不要创建 .env、config.js 或任何存放 key 的文件并提交。**

**4. 浏览器直连 Anthropic API 需要特定请求头**
必须带 `anthropic-dangerous-direct-browser-access: true`，否则 CORS 拦截。当前用 `claude-sonnet-4-6`。

**5. iOS 细节**
video 标签必须有 `playsinline` 和 `muted`，否则 iOS 会强制全屏播放。布局用 `env(safe-area-inset-*)` 处理刘海和底部横条。

---

**6. 存档只在本地，而且可能被系统回收**
照片存在 IndexedDB，只在这台设备上，不上传。但 Safari 对一阵子没访问的站点会回收本地数据——
这跟「三周没走也不会归零」的勋章原则直接冲突。缓解办法：把网页「添加到主屏幕」（iOS 会当成 App 对待，
保留得牢得多），以及存档页底部的导出备份。都不是保证，设计上必须假设数据会没。

**7. 数据结构拆成两个仓库是有原因的**
`walks` 一趟一条（含 240px 缩略图，约 27KB），`photos` 一张大图一条。
打开存档只读 `walks`，点进某一趟才读大图。不拆的话打开存档要把几十兆全捞出来，手机会卡死。
DB 版本改过一次（1→2 加 `meta` 仓库存跨时间观察），升级逻辑用 `if(!contains)` 守着，老数据不丢。

**8. 换模型比想的容易，但 key 仍然不能进代码**
DeepSeek 有兼容 Anthropic 格式的接口 `https://api.deepseek.com/anthropic`，
实测**允许浏览器直连**（CORS 放行 klu991230.github.io），视觉模型是 `deepseek-v4-flash-vision-exp`。
两家配置收在 `PROVIDERS` 常量里，首页可以直接切。
**但这不改变约束 3**：静态页面没有服务器，任何写进代码的 key 都等于公开发布。
**中转站已经建好了**（2026-08-31），所以现在打开网页就有 AI 点评，不用填 key：

- 代码 `proxy/worker.js`，部署说明 `proxy/README.md`
- 跑在 Cloudflare Workers，地址 `https://walk-ai.lutszchin1999.workers.dev`
- DeepSeek 的 key 存在 Cloudflare 的 Secret 里（变量名 `DEEPSEEK_KEY`），浏览器和仓库里都没有
- 中转站地址本身**不是秘密**，可以进代码——秘密的是它服务器上那把 key
- 只放行来自 `klu991230.github.io` 的请求，模型写死、`max_tokens` 封顶、强制关思考模式
- App 里的逻辑：填了自己 key 的走自己的，没填的走中转站（`useProxy()` / `aiReady()`）

中转站不存数据，不违反「不上真后端」——那条说的是别为存数据上后端。

**来源检查挡得住浏览器里的滥用，挡不住命令行。** 真正的兜底是 DeepSeek 后台的余额上限。
要换 key 就去 Cloudflare 改那个 Secret，App 一个字都不用动。

**9. DeepSeek 默认开着思考模式，会把 max_tokens 吃光**
`deepseek-v4-flash-vision-exp` 默认先在 `thinking` 块里推理，再在 `text` 块里说正文。
想得久了就把 `max_tokens` 用完，返回里只剩思考块、`text` 是空的——
界面上的表现是**转圈转完什么都没有**，而且时有时无（想得短的时候够用）。

所以给 DeepSeek 固定带 `thinking:{type:"disabled"}`（`PROVIDERS` 里的 `noThink`）。
我们要的是一句 45 字的俏皮话，不需要它推理。
万一哪天这个参数被拒（400），`callAI` 会去掉它重发一次，不让点评功能整个挂掉。

**10. 空字符串不能当成功返回**
`callAI` 里取不到文本就抛错并说明原因（字数用完 / 只顾着想 / 拒绝回答 / 空的）。
宁可在界面上显示一行错误，也不能留一片白——
**留白用户会以为是 App 坏了，而不是这一次调用失败了。** 上面那个 bug 就是这么被拖了很久才发现的。

---

## 现有设计（这些是想清楚过的，改之前先问）

### 任务设计的核心原则
**好任务改变的是姿势和视角，不是路线。**

「拍一处有人手写的字」是好任务——在哪都能做，改变你怎么看。
「拍 5 家肯德基」是坏任务——要走到特定地点，散步就变成跑腿了。

**新增任务必须过三关**（`tasks.json` 开头也写着）：
1. 改变的是姿势和视角，还是逼人走到特定地点？后者一律不要
2. 站在任何一条路上都能做到吗？依赖「刚好有」的东西不要
3. 两三秒内能做完吗？需要维持怪姿势、旁人会盯着你看的，不要

### 三组任务，难度递进
现在叫「**挑刺 / 折腰 / 出神**」，题目在 `tasks.json` 里，每组 10 条。
（名字改过两轮：看/动/想 → 找茬/出洋相/发呆 → 挑刺/折腰/出神。
代码里 `ROUND_RENAME` 负责把存档里的老记录迁过来，只改组名不动任务原文。）

- **挑刺** —— 找瑕疵、找不对劲的地方。改变的是**你看什么**
- **折腰** —— 改变**你从哪里看**。「折腰」是个玩笑：不为五斗米折腰，你为一个井盖折了
- **出神** —— 得停下来想一会儿才拍得出来。改变的是**你看多久**

改题目直接在 GitHub 网页上编辑 `tasks.json` 即可，不用碰代码，一两分钟后全网生效。
代码里 `FALLBACK` 常量是兜底题库（每组一条），json 读不到或被编辑坏时用它，保证不白屏。**别删。**

### ⚠️ 一个被推翻了的赌（2026-08-31）

原来这份文档写着：「动」这一组押注最重，赌的是**身体动作本身就是产品**，
成年人已经很多年没蹲在路边看过东西了。

**作者真机走过之后的结论是：做不出来。太放不开。**

所以那一组从「出洋相」重写成了「折腰」，规则变成：

> **尴尬是成本，不是产品。**
> 成本太高你就不做，不做就一张都拍不到——那个真正想要的视角变化也一起没了。

砍掉的是：举过头顶盲拍、原地转一圈、贴墙站好、手机贴地拍虫子视角。
留下的是：蹲下、抬头、回头、找反光面、后退十步、把手机倒过来拿、
凑近拍纹理、从旁边探出去、走另一侧。

**判断标准：两三秒内能做完、不需要维持怪姿势、旁人根本不会注意到。**

### AI 语气：有点损的朋友（对外叫「小陆辣评」）
界面上所有点评都挂在「小陆辣评」这个名字下。
**这个前缀只在显示时加**（`replyHTML()`），存进存档的 `reply` 必须保持干净——
那些原文会被原样喂回给 AI 做跨时间观察，掺进标签就是污染输入。

提示词在 `VOICE` 常量里。三条硬约束：
1. 说出照片里一个用户**没注意到**的细节（不是描述照片内容）
2. 顺带轻损一句
3. 一到两句，最多 45 字

**红线：可以损照片、损地方、损他挑的对象，不能损他的努力和审美。** 越线就从玩笑变成打击，用户会不想拍第二张。

**还有一条同等重要的：不许判卷。** 不评完成度、不说"没拍对/不算/偏题了"。
这跟「刻意没做的东西」里那条「对错判断 / 完成度评分」是同一件事。

> 踩过一次（2026-08-31）：为了让点评扣住题干，提示词里写了
> 「他挑的这个对象配不配得上这个任务」，模型顺着就开始打分，
> 说出了「三张照片没一张拍对重点，你这双眼睛是装饰用的吧」——
> 两条红线一起踩了。
>
> 改法是把方向从**评判**换成**指路**：
> 说的是「那边还藏着一个更够劲的」，不是「你那个不算」。
> 提示词里现在明确禁掉了判卷用词和挖苦眼力的说法。

### 刻意没做的东西（别加）
- **连续打卡 / streak** — 会把玩变成作业，断掉那天就卸载
- **对错判断 / 完成度评分** — 误判的代价极高，拍到了却被判不算，就不想拍第二张了
- 排行榜、好友系统、地图轨迹

---

## v2 进度

### ✅ 1. 题库拆成独立 json（已上线）
题库在 `tasks.json`，页面启动时 fetch，带时间戳绕开手机缓存。
读不到或格式不对时退回代码里的 `FALLBACK`（每组一条），App 不白屏。
每组从 4 条扩到 10 条，「换一个」的备选从 3 个变成 9 个。

**新增任务必须过「改变姿势，不改变路线」这条测试。**

### ✅ 2. 三组改名（已上线）
看 / 动 / 想 → **找茬 / 出洋相 / 发呆**

### ✅ 3. 底栏两个 tab（已上线）
**走** 和 **存档**。底栏只在「欢迎 / 存档 / 结束卡片 / 某一趟详情 / 这阵子」显示；
过场、取景、回看时收起来——走路途中不该有别的地方可去。
结束卡片显示，是因为那趟已经走完了，得留一条去存档的路。

### ✅ 4. 存档 + 跨时间观察（已上线，这是 v2 的核心价值）
走完自动存进 IndexedDB，存档 tab 是时间线，点进某一趟看大图，可以删。

「这阵子」这一屏分两半，性质完全不同：
- **数出来的**：纯本地计算，不联网、不要 key、永远准。走了几趟、做过多少个不同的任务、
  抬头/蹲下/回头各几张。「12 个任务你做全了」那类勋章的数据基础已经在这儿了。
- **AI 看出来的**：把过去所有的任务名和 AI 当时说过的话当成**纯文字**发过去找规律，
  **不发图片**。实测 12 趟只有 1867 字，一百趟也放得下。
  一百张图又慢又贵还超容量，而每张的点评本来就是那张照片的压缩描述。
  门槛 10 趟，手动按钮，结果存进 `meta` 仓库，之后又走了几趟会提示重新生成。

底部还有**导出 / 恢复备份**（原本排在第 6，因为 Safari 回收数据的风险提前做了）。
恢复是合并不是覆盖，重复导入不会翻倍。

### ❌ 5. 勋章：奖励探索，不奖励勤奋（没做）
> `localStats()` 里的 `covered` 就是「题库里做过几个」，只算现在题库里还有的题，
> 历史上做过、后来下架的老题单独算 `retired`——「任务做全了」这类勋章必须用前者，
> 用错了勋章就是假的。

**不做连续打卡。** 做这类：
- 「12 个任务你做全了」
- 「影子你拍过 5 次」
- 「你在雨天走过」

判断标准：**三周没走也不会归零的，才能做勋章。**

数据都已经在存档里了，`localStats()` 那个函数就是现成的起点。

### ❌ 6. 分享（没做）
把结束卡片渲染成一张图片存到相册（canvas 导出即可），用户自己发到哪都行。
不做站内分享，不需要后端。
（导出备份已经做了，那是防丢；这一条是给人看的，两回事。）

### ✅ 模型可切换：Claude / DeepSeek（已上线）
首页 key 那一栏可以选哪家，两家的 key 分开存在 localStorage，来回切不用重填。

两家配置在 `PROVIDERS` 常量里，只有地址和模型名不同——DeepSeek 提供了兼容 Anthropic
格式的接口，请求体、图片怎么塞全都一样。DeepSeek 用 `deepseek-v4-flash-vision-exp`
（只有这个读得了图）。`anthropic-dangerous-direct-browser-access` 这个头是 Anthropic
特有的，只发给 Anthropic。

加这个开关的目的不只是省钱和提速——**是要验证换个模型那股「损朋友」的劲儿还在不在**。
这件事还没有答案。

## 建议的做法

- 一次做一件，做完提交一次，别一口气全上
- 优先级 1→2 是小改动，可以先做完验证发布流程通不通
- 4 是最大的一块，做之前先把数据结构讲清楚给作者听
- 每次改完提醒作者在手机上真机走一趟，这个 App 的问题只有走路时才暴露

---

## 已知的小问题

- 手机上偶尔加载到缓存的旧版本，网址后加 `?2` 可强制刷新。`tasks.json` 已经带时间戳，
  但 `index.html` 本身还没有版本号
- 目前没有任何错误上报，AI 调用失败只在界面上显示一行字
- 数码变焦 3 倍时只用了原图中间九分之一的像素，照片会明显糊。上限压在 3 倍是这个原因。
  iOS Safari 基本不支持硬件变焦，安卓上代码会自动切过去

---

## 待验证的问题（作者还没给出答案）

- 「损」的度对不对：走过的几趟都没填 key，实际上一次 AI 点评都没看到。**到现在仍是未验证状态，
  这是最该先解决的一件事。**
- 跨时间观察那几句话，会不会真的说出他没想到的东西——这功能是整个 v2 的理由，但还没被真实数据检验过
- 换成 DeepSeek 之后，那股「损」还在不在
- 「出洋相」那组是不是三组里最好玩的——押注在这，但还没被证实
- AI 那句点评用户会不会想截图——这是判断产品成不成立的核心指标
