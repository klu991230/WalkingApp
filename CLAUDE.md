# 散步 App — 项目交接文档

给 Claude Code 的上下文。项目已上线运行，这是继续迭代的起点。

---

## 一句话

一个把散步变成拍照任务的小游戏。开局给三个任务，用手机实时取景拍照，AI 用「有点损的朋友」的语气点评，走完生成一张卡片。

---

## 当前状态

**已上线**：https://klu991230.github.io/WalkingApp/
**仓库**：github.com/klu991230/WalkingApp（Public）
**部署**：GitHub Pages，main 分支根目录，push 后一两分钟自动更新
**代码**：单个 `index.html`，约 450 行，HTML + CSS + 原生 JS，无框架、无构建步骤、无依赖

v1 已经真机走通：手机上实时取景、拍照、AI 点评、生成卡片，全流程正常。

---

## 关于作者（重要）

- **非技术背景**，咨询顾问，不会从零写代码
- 目标是**看懂代码在干什么**，不是自己写
- **给命令和代码之前，先用中文讲清楚每一段在做什么**，讲完再给。他说"just the commands"时才跳过讲解
- 界面配色**偏好浅色、米色系，不要深色**
- 这个项目定位是**自己动手练习**，不是创业项目。别往商业化方向带

---

## 技术约束（都是踩过的坑，别踩回去）

**1. 实时取景必须 HTTPS**
`getUserMedia` 只在 https 或 localhost 下可用。本地测试用 `python3 -m http.server` 起服务后走 `localhost` 可以；用局域网 IP（192.168.x.x）访问会被浏览器拒绝。代码里已有兜底：摄像头调不起来时自动退回系统相机（file input + capture）。**别删这个兜底。**

**2. 不能引外部字体和 CDN**
作者在中国大陆，Google Fonts 等境外 CDN 不可靠。**只用系统字体**，现在是 PingFang SC + ui-monospace。别引入任何 npm 包或 CDN 链接。

**3. API key 绝对不能进仓库**
仓库是 Public。key 由用户填在页面输入框里，存 localStorage。代码里有个 `HARDCODED_KEY` 常量必须保持空字符串。**不要创建 .env、config.js 或任何存放 key 的文件并提交。**

**4. 浏览器直连 Anthropic API 需要特定请求头**
必须带 `anthropic-dangerous-direct-browser-access: true`，否则 CORS 拦截。当前用 `claude-sonnet-4-6`。

**5. iOS 细节**
video 标签必须有 `playsinline` 和 `muted`，否则 iOS 会强制全屏播放。布局用 `env(safe-area-inset-*)` 处理刘海和底部横条。

---

## 现有设计（这些是想清楚过的，改之前先问）

### 任务设计的核心原则
**好任务改变的是姿势和视角，不是路线。**

「拍一处有人手写的字」是好任务——在哪都能做，改变你怎么看。
「拍 5 家肯德基」是坏任务——要走到特定地点，散步就变成跑腿了。

**新增任务必须通过这条测试。**

### 三组任务，难度递进
当前叫「看 / 动 / 想」，写死在 `POOL` 对象里，每组 4 条。

「动」这一组是设计上押注最重的——蹲下来、抬头、回头。赌的是**这个身体动作本身就是产品**，成年人已经很多年没蹲在路边看过东西了。

### AI 语气：有点损的朋友
提示词在 `VOICE` 常量里。三条硬约束：
1. 说出照片里一个用户**没注意到**的细节（不是描述照片内容）
2. 顺带轻损一句
3. 一到两句，最多 45 字

**红线：可以损照片、损地方、损他挑的对象，不能损他的努力和审美。** 越线就从玩笑变成打击，用户会不想拍第二张。

### 刻意没做的东西（别加）
- **连续打卡 / streak** — 会把玩变成作业，断掉那天就卸载
- **对错判断 / 完成度评分** — 误判的代价极高，拍到了却被判不算，就不想拍第二张了
- 排行榜、好友系统、地图轨迹

---

## v2 要做的（按优先级）

### 1. 题库拆成独立 json
现在题库写死在 HTML 里。拆成 `tasks.json`，页面启动时 fetch。

**目的**：改题目不用碰代码，在 GitHub 网页上编辑一个纯文本文件即可，一两分钟后全网生效。

**不要上真后端。** 对当前规模，一个 json 文件就是后端。真正需要服务端的只有「分享给朋友」和「换手机数据还在」，其余都不需要。

顺便把题库从每组 4 条扩到每组 10 条左右。新题必须过「改变姿势不改变路线」这条测试。

### 2. 三组改名
「看 / 动 / 想」太素。作者选的方向是更贴损朋友语气的一组：

**找茬 / 出洋相 / 发呆**

（「出洋相」是对「蹲在路边拍井盖」最诚实的描述。）

### 3. 底栏两个 tab
- **走** — 现在的流程，开始新的一趟
- **存档** — 时间线，按日期列出每趟散步和照片

暂时只做两个 tab。

### 4. 存档 + 跨时间观察（这是 v2 的核心价值）
用 **IndexedDB** 存在本地（照片是 base64，localStorage 的 5MB 上限装不下）。不需要账号，离线可用。

**关键功能不是仓库，是回看时的观察**：攒够一定数量后，让 AI 横着看所有照片，指出模式——
- 「你 40 张里有 12 张是往上拍的」
- 「你特别爱拍生锈的东西」
- 「你这个月的照片比上个月安静」

**这才是作者要的 reflect。** 单张照片能说的话很有限，一百张才有话说。这个功能是整个 v2 的理由。

### 5. 勋章：奖励探索，不奖励勤奋
**不做连续打卡。** 做这类：
- 「12 个任务你做全了」
- 「影子你拍过 5 次」
- 「你在雨天走过」

判断标准：**三周没走也不会归零的，才能做勋章。**

### 6. 分享
把结束卡片渲染成一张图片存到相册（canvas 导出即可），用户自己发到哪都行。不做站内分享，不需要后端。

---

## 建议的做法

- 一次做一件，做完提交一次，别一口气全上
- 优先级 1→2 是小改动，可以先做完验证发布流程通不通
- 4 是最大的一块，做之前先把数据结构讲清楚给作者听
- 每次改完提醒作者在手机上真机走一趟，这个 App 的问题只有走路时才暴露

---

## 已知的小问题

- 手机上偶尔加载到缓存的旧版本，网址后加 `?2` 可强制刷新。如果频繁出现，考虑给资源加版本号
- 目前没有任何错误上报，AI 调用失败只在界面上显示一行字

---

## 待验证的问题（作者还没给出答案）

- 「损」的度对不对：走了一趟说"挺好的没啥问题"，但那趟没填 key，实际上没看到 AI 点评。**这个还是未验证状态。**
- 「出洋相」那组是不是三组里最好玩的——押注在这，但还没被证实
- AI 那句点评用户会不会想截图——这是判断产品成不成立的核心指标
